The Project is about openning a backdoor in the victim device to be able to use shell , while retrieving data outputs into a NOSQL Mongodb database

--> server_ is a python server socket that uses db to store victim outputs 
--> client_ is the client file that open the tcp session and store it's output in mongodb

--> simple_terminal.py  .. you can implement it into client_ if you want more efficiency , as client_ is just a sample of client socket with db idea 
